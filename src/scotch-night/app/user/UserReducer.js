import { USER_UPDATED } from './UserActionTypes';

const userReducer = (state = [], action) => {
    switch (action.type) {
        default: {
            return state;
        }
    }
};

export default userReducer;
